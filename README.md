# Noren Focus

A minimalist focus timer and three-priority daily planner. Data stays in the
browser through `localStorage`; there is no account, analytics, or backend.

## Development

```bash
npm install
npm run dev
```

Then open `http://localhost:3000`.

## Production

```bash
npm run build
npm start
```
